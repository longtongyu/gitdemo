# Git工作流程详解

## Git的四个区域

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   工作区         │    │   暂存区         │    │  本地仓库        │    │  远程仓库        │
│ Working Dir     │    │ Staging Area    │    │ Local Repo      │    │ Remote Repo     │
│                 │    │                 │    │                 │    │                 │
│ 你正在编辑的文件  │───▶│ git add 添加的文件│───▶│ git commit 提交  │───▶│ git push 推送    │
│                 │    │                 │    │                 │    │                 │
│ 修改但未暂存     │    │ 准备提交的文件    │    │ 已提交的版本     │    │ 团队共享的版本   │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 详细流程说明

### 1. 工作区 (Working Directory)
- 你实际编辑文件的地方
- 文件在这里被修改、创建、删除
- 使用 `git status` 查看工作区状态

### 2. 暂存区 (Staging Area)
- 临时存储准备提交的文件
- 使用 `git add` 将文件从工作区添加到暂存区
- 可以分批次添加文件

### 3. 本地仓库 (Local Repository)
- 存储项目的所有版本历史
- 使用 `git commit` 将暂存区的文件提交到本地仓库
- 每次提交都会创建一个新的版本记录

### 4. 远程仓库 (Remote Repository)
- 存储在远程服务器上的仓库（如GitHub）
- 使用 `git push` 将本地仓库的提交推送到远程
- 团队成员可以共享和协作

## 完整工作流程示例

```bash
# 1. 修改文件（在工作区）
echo "新内容" >> file.txt

# 2. 查看状态
git status  # 显示工作区的修改

# 3. 添加到暂存区
git add file.txt  # 或 git add . 添加所有文件

# 4. 再次查看状态
git status  # 显示暂存区的文件

# 5. 提交到本地仓库
git commit -m "添加新内容"

# 6. 推送到远程仓库
git push origin master
``` 